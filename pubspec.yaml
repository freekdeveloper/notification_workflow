name: notification_workflow
description: "Notification Workflow Example App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  hooks_riverpod: ^3.3.1
  go_router: ^17.2.3
  intl: ^0.20.2
  flutter_dotenv: ^6.0.1
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  environment_config: ^4.0.0+1

flutter:
  uses-material-design: true
